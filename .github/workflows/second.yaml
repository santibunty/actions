name: My First Workflow

on: push

jobs: 
    first_job: 
        runs-on: ubuntu-latest
        steps: 
            - uses: actions/checkout@v5
            - name: Executing shell script
              run: ./ascii.sh
